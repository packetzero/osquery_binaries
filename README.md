# Osquery Dependencies

This is a collection of miscellaneous binary builds of required dependencies for osquery development.  For example, on windows, the aws-sdk-cpp build when updating to a new release.  

